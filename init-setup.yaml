---
- hosts: localhost
  connection: local
  vars_files:
  - ./vars/vars.yaml

  roles:
    - role: add-stereum-user
    - role: configure-timesyncd
    - role: install-docker
    - role: install-e2dc
    - role: switch-to-network
    - role: set-setup
    - role: set-public-ip
    - role: start-services
    - role: git-update
    - role: git-update-fetch-merge
    - role: stop-all-containers
      start-services: "{{ setups[setup] }}"
